const myNum = [1, 2, 3]

// const myTotol = myNum.reduce((acc, curVal) => {
//     console.log(`acc: ${acc} and curval: ${curVal}`);
//     return acc + curVal;
// }, 0)

// const myTotol = myNum.reduce((acc, curVal) => acc + curVal, 5)
// console.log(myTotol);

const shoppingCart = [
    {
        itemName: "JS course", 
        price: 2999
    },
    {
        itemName: "CSS course", 
        price: 2000
    },
    {
        itemName: "HTML course", 
        price: 1500
    },
    {
        itemName: "Python course", 
        price: 3000
    }
]

// const totalPrice = shoppingCart.reduce((acc, item) => acc + item.price, 0)
// console.log(totalPrice);
